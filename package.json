{
  "name": "oidc-basic-trans-worker",
  "version": "0.0.1",
  "devDependencies": {
    "wrangler": "3.1.1"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev -l",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "cookie": "^0.5.0",
    "itty-router": "^2.6.1"
  }
}
